import React, { Component } from "react";
import "./static/style.css";

class App extends Component {
  // Function called when a mole is hit
  // hit = () => { };

  // Change the game's status
  // startGame = () => { };

  // Make a new mole appear randomly
  // changeActiveMole = () => { };

  render() {
    return (
      <div className="app">
        {/* Change this */}
        Hello World !
      </div>
    );
  }
}

export default App;
